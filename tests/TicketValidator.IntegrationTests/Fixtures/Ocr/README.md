# OCR Fixture

`sharp-ticket.png` is a fictitious, high-contrast ticket used by
`TesseractOcrServiceTests`. It contains no personal or real business data and
uses the visible word `TICKETVALIDATOR` as a stable OCR assertion.
